

function(conan_message MESSAGE_OUTPUT)
    if(NOT CONAN_CMAKE_SILENT_OUTPUT)
        message(${ARGV${0}})
    endif()
endfunction()


macro(conan_find_apple_frameworks FRAMEWORKS_FOUND FRAMEWORKS FRAMEWORKS_DIRS)
    if(APPLE)
        foreach(_FRAMEWORK ${FRAMEWORKS})
            # https://cmake.org/pipermail/cmake-developers/2017-August/030199.html
            find_library(CONAN_FRAMEWORK_${_FRAMEWORK}_FOUND NAME ${_FRAMEWORK} PATHS ${FRAMEWORKS_DIRS})
            if(CONAN_FRAMEWORK_${_FRAMEWORK}_FOUND)
                list(APPEND ${FRAMEWORKS_FOUND} ${CONAN_FRAMEWORK_${_FRAMEWORK}_FOUND})
            else()
                message(FATAL_ERROR "Framework library ${_FRAMEWORK} not found in paths: ${FRAMEWORKS_DIRS}")
            endif()
        endforeach()
    endif()
endmacro()


function(conan_package_library_targets libraries package_libdir deps out_libraries out_libraries_target build_type package_name)
    unset(_CONAN_ACTUAL_TARGETS CACHE)
    unset(_CONAN_FOUND_SYSTEM_LIBS CACHE)
    foreach(_LIBRARY_NAME ${libraries})
        find_library(CONAN_FOUND_LIBRARY NAME ${_LIBRARY_NAME} PATHS ${package_libdir}
                     NO_DEFAULT_PATH NO_CMAKE_FIND_ROOT_PATH)
        if(CONAN_FOUND_LIBRARY)
            conan_message(STATUS "Library ${_LIBRARY_NAME} found ${CONAN_FOUND_LIBRARY}")
            list(APPEND _out_libraries ${CONAN_FOUND_LIBRARY})
            if(NOT ${CMAKE_VERSION} VERSION_LESS "3.0")
                # Create a micro-target for each lib/a found
                set(_LIB_NAME CONAN_LIB::${package_name}_${_LIBRARY_NAME}${build_type})
                if(NOT TARGET ${_LIB_NAME})
                    # Create a micro-target for each lib/a found
                    add_library(${_LIB_NAME} UNKNOWN IMPORTED)
                    set_target_properties(${_LIB_NAME} PROPERTIES IMPORTED_LOCATION ${CONAN_FOUND_LIBRARY})
                    set(_CONAN_ACTUAL_TARGETS ${_CONAN_ACTUAL_TARGETS} ${_LIB_NAME})
                else()
                    conan_message(STATUS "Skipping already existing target: ${_LIB_NAME}")
                endif()
                list(APPEND _out_libraries_target ${_LIB_NAME})
            endif()
            conan_message(STATUS "Found: ${CONAN_FOUND_LIBRARY}")
        else()
            conan_message(STATUS "Library ${_LIBRARY_NAME} not found in package, might be system one")
            list(APPEND _out_libraries_target ${_LIBRARY_NAME})
            list(APPEND _out_libraries ${_LIBRARY_NAME})
            set(_CONAN_FOUND_SYSTEM_LIBS "${_CONAN_FOUND_SYSTEM_LIBS};${_LIBRARY_NAME}")
        endif()
        unset(CONAN_FOUND_LIBRARY CACHE)
    endforeach()

    if(NOT ${CMAKE_VERSION} VERSION_LESS "3.0")
        # Add all dependencies to all targets
        string(REPLACE " " ";" deps_list "${deps}")
        foreach(_CONAN_ACTUAL_TARGET ${_CONAN_ACTUAL_TARGETS})
            set_property(TARGET ${_CONAN_ACTUAL_TARGET} PROPERTY INTERFACE_LINK_LIBRARIES "${_CONAN_FOUND_SYSTEM_LIBS};${deps_list}")
        endforeach()
    endif()

    set(${out_libraries} ${_out_libraries} PARENT_SCOPE)
    set(${out_libraries_target} ${_out_libraries_target} PARENT_SCOPE)
endfunction()


include(FindPackageHandleStandardArgs)

conan_message(STATUS "Conan: Using autogenerated Findlibxrandr.cmake")
# Global approach
set(libxrandr_FOUND 1)
set(libxrandr_VERSION "1.5.2")

find_package_handle_standard_args(libxrandr REQUIRED_VARS
                                  libxrandr_VERSION VERSION_VAR libxrandr_VERSION)
mark_as_advanced(libxrandr_FOUND libxrandr_VERSION)


set(libxrandr_INCLUDE_DIRS "/home/pierre/.conan/data/libxrandr/1.5.2/bincrafters/stable/package/ad69a4d3c01840b0a407a101a03b1987c4a0eb48/include")
set(libxrandr_INCLUDE_DIR "/home/pierre/.conan/data/libxrandr/1.5.2/bincrafters/stable/package/ad69a4d3c01840b0a407a101a03b1987c4a0eb48/include")
set(libxrandr_INCLUDES "/home/pierre/.conan/data/libxrandr/1.5.2/bincrafters/stable/package/ad69a4d3c01840b0a407a101a03b1987c4a0eb48/include")
set(libxrandr_RES_DIRS )
set(libxrandr_DEFINITIONS )
set(libxrandr_LINKER_FLAGS_LIST
        $<$<STREQUAL:$<TARGET_PROPERTY:TYPE>,SHARED_LIBRARY>:>
        $<$<STREQUAL:$<TARGET_PROPERTY:TYPE>,EXECUTABLE>:>
)
set(libxrandr_COMPILE_DEFINITIONS )
set(libxrandr_COMPILE_OPTIONS_LIST "" "")
set(libxrandr_LIBRARIES_TARGETS "") # Will be filled later, if CMake 3
set(libxrandr_LIBRARIES "") # Will be filled later
set(libxrandr_LIBS "") # Same as libxrandr_LIBRARIES
set(libxrandr_SYSTEM_LIBS )
set(libxrandr_FRAMEWORK_DIRS )
set(libxrandr_FRAMEWORKS )
set(libxrandr_FRAMEWORKS_FOUND "") # Will be filled later
set(libxrandr_BUILD_MODULES_PATHS )

conan_find_apple_frameworks(libxrandr_FRAMEWORKS_FOUND "${libxrandr_FRAMEWORKS}" "${libxrandr_FRAMEWORK_DIRS}")

mark_as_advanced(libxrandr_INCLUDE_DIRS
                 libxrandr_INCLUDE_DIR
                 libxrandr_INCLUDES
                 libxrandr_DEFINITIONS
                 libxrandr_LINKER_FLAGS_LIST
                 libxrandr_COMPILE_DEFINITIONS
                 libxrandr_COMPILE_OPTIONS_LIST
                 libxrandr_LIBRARIES
                 libxrandr_LIBS
                 libxrandr_LIBRARIES_TARGETS)

# Find the real .lib/.a and add them to libxrandr_LIBS and libxrandr_LIBRARY_LIST
set(libxrandr_LIBRARY_LIST Xrandr)
set(libxrandr_LIB_DIRS "/home/pierre/.conan/data/libxrandr/1.5.2/bincrafters/stable/package/ad69a4d3c01840b0a407a101a03b1987c4a0eb48/lib")

# Gather all the libraries that should be linked to the targets (do not touch existing variables):
set(_libxrandr_DEPENDENCIES "${libxrandr_FRAMEWORKS_FOUND} ${libxrandr_SYSTEM_LIBS} libxrender::libxrender;libxext::libxext")

conan_package_library_targets("${libxrandr_LIBRARY_LIST}"  # libraries
                              "${libxrandr_LIB_DIRS}"      # package_libdir
                              "${_libxrandr_DEPENDENCIES}"  # deps
                              libxrandr_LIBRARIES            # out_libraries
                              libxrandr_LIBRARIES_TARGETS    # out_libraries_targets
                              ""                          # build_type
                              "libxrandr")                                      # package_name

set(libxrandr_LIBS ${libxrandr_LIBRARIES})

foreach(_FRAMEWORK ${libxrandr_FRAMEWORKS_FOUND})
    list(APPEND libxrandr_LIBRARIES_TARGETS ${_FRAMEWORK})
    list(APPEND libxrandr_LIBRARIES ${_FRAMEWORK})
endforeach()

foreach(_SYSTEM_LIB ${libxrandr_SYSTEM_LIBS})
    list(APPEND libxrandr_LIBRARIES_TARGETS ${_SYSTEM_LIB})
    list(APPEND libxrandr_LIBRARIES ${_SYSTEM_LIB})
endforeach()

# We need to add our requirements too
set(libxrandr_LIBRARIES_TARGETS "${libxrandr_LIBRARIES_TARGETS};libxrender::libxrender;libxext::libxext")
set(libxrandr_LIBRARIES "${libxrandr_LIBRARIES};libxrender::libxrender;libxext::libxext")

set(CMAKE_MODULE_PATH "/home/pierre/.conan/data/libxrandr/1.5.2/bincrafters/stable/package/ad69a4d3c01840b0a407a101a03b1987c4a0eb48/"
			"/home/pierre/.conan/data/libxrandr/1.5.2/bincrafters/stable/package/ad69a4d3c01840b0a407a101a03b1987c4a0eb48/lib/pkgconfig" ${CMAKE_MODULE_PATH})
set(CMAKE_PREFIX_PATH "/home/pierre/.conan/data/libxrandr/1.5.2/bincrafters/stable/package/ad69a4d3c01840b0a407a101a03b1987c4a0eb48/"
			"/home/pierre/.conan/data/libxrandr/1.5.2/bincrafters/stable/package/ad69a4d3c01840b0a407a101a03b1987c4a0eb48/lib/pkgconfig" ${CMAKE_PREFIX_PATH})

foreach(_BUILD_MODULE_PATH ${libxrandr_BUILD_MODULES_PATHS})
    include(${_BUILD_MODULE_PATH})
endforeach()

if(NOT ${CMAKE_VERSION} VERSION_LESS "3.0")
    # Target approach
    if(NOT TARGET libxrandr::libxrandr)
        add_library(libxrandr::libxrandr INTERFACE IMPORTED)
        if(libxrandr_INCLUDE_DIRS)
            set_target_properties(libxrandr::libxrandr PROPERTIES INTERFACE_INCLUDE_DIRECTORIES
                                  "${libxrandr_INCLUDE_DIRS}")
        endif()
        set_property(TARGET libxrandr::libxrandr PROPERTY INTERFACE_LINK_LIBRARIES
                     "${libxrandr_LIBRARIES_TARGETS};${libxrandr_LINKER_FLAGS_LIST}")
        set_property(TARGET libxrandr::libxrandr PROPERTY INTERFACE_COMPILE_DEFINITIONS
                     ${libxrandr_COMPILE_DEFINITIONS})
        set_property(TARGET libxrandr::libxrandr PROPERTY INTERFACE_COMPILE_OPTIONS
                     "${libxrandr_COMPILE_OPTIONS_LIST}")
        
        # Library dependencies
        include(CMakeFindDependencyMacro)

        if(NOT libxrender_FOUND)
            find_dependency(libxrender REQUIRED)
        else()
            message(STATUS "Dependency libxrender already found")
        endif()


        if(NOT libxext_FOUND)
            find_dependency(libxext REQUIRED)
        else()
            message(STATUS "Dependency libxext already found")
        endif()

    endif()
endif()
